/*
 * The main function is always the first code that runs in every executable Rust program.
 */

fn main() {
    /*
     * This line prints "Hello, world!" in the console.
     * Don't mind the !. It's an indicator that we're calling a macro, instead of a function.
     * We'll cover macros later on.
     */
    println!("Hello, world!");
}
